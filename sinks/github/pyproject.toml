[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "observe-github"
version = "0.1.0"
description = "GitHub issue sink for observe"
requires-python = ">=3.11"
# The consumer picks where `observe` comes from via [tool.uv.sources].
dependencies = [
    "observe",
]

[project.optional-dependencies]
# Signing a GitHub App JWT needs RSA, which the standard library has not got.
# Only installs that authenticate as an App pay for it.
app = [
    "cryptography>=42",
]
dev = [
    "pytest>=8",
    "cryptography>=42",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
observe_github = ["py.typed"]
